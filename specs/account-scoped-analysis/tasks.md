# Tareas: análisis por ámbito de cuenta

- [x] **1. Crear el esquema SQLite v3.** Añadir `accounts`, `statements.account_id`, CHECK de identidad, índices parciales que distingan moneda nula de vacía e índice por cuenta/período para bases nuevas. Validación: `tests/test_persistence.py` confirma restricciones, FK, índices y `user_version = 3`.
- [x] **2. Implementar migraciones a v3.** Migrar bases v1 y v2, agrupando identidades visibles y aislando identidades ambiguas dentro de una única transacción orquestada. Validación: `tests/test_persistence.py` conserva cantidades y relaciones, y comprueba rollback v3 desde v1 y v2 con su versión inicial.
- [x] **3. Asignar y limpiar ámbitos en escrituras.** Adaptar `save()` para encontrar o crear cuentas y `delete()` para conservar o eliminar el ámbito según queden cartolas. Validación: `tests/test_persistence.py` cubre carga concurrente compatible con moneda presente o nula, cuenta ambigua, eliminación parcial y eliminación final.
- [x] **4. Añadir catálogo e historial por cuenta.** Implementar `list_accounts()`, `get_account()` y `history_for_account()` con agregados, orden exacto y filtro SQL de intersección temporal; eliminar `history_for_anchor()`. Validación: `tests/test_persistence.py` distingue cuenta inexistente de rango vacío y cubre catálogo vacío/múltiple, más de cien cartolas y exclusión de períodos ajenos.
- [x] **5. Reemplazar el contrato HTTP.** Añadir `GET /v1/accounts`, normalizar moneda de catálogo, exigir `account_id` en análisis, añadirlo a `scope` y rechazar selectores obsoletos o parámetros duplicados. Validación: `tests/test_api.py` cubre monedas conocida/nula/vacía/no soportada, éxito, autenticación, errores 400/404/409, privacidad y ausencia de escrituras.
- [x] **6. Completar defensa de dominio y backup.** Añadir casos explícitos de banco, producto, moneda e identidad incompatibles, y verificar backup/restauración v3. Validación: `tests/test_analysis.py` y `tests/test_backup.py` pasan sin alterar fórmulas existentes.
- [x] **7. Actualizar documentación y verificar fase 12.** Actualizar README, esquema de base y roadmap; ejecutar suite Python completa y smoke HTTP. Validación: CA-01 a CA-10 están cubiertos por las pruebas de persistencia, API, análisis y backup; `134 passed` y el smoke HTTP terminan correctamente.
