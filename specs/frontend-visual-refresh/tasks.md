# Tareas: renovación visual de la interfaz

- [ ] **1. Añadir la fuente local y el tema MUI.** Crear `frontend/src/theme.ts`, instalar `@fontsource-variable/ibm-plex-sans` e integrar fuente, paleta morada, tipografía, foco y overrides MUI en `main.tsx`. Validación: `npm run build` aplica el tema sin recursos tipográficos externos.
- [x] **2. Corregir metadatos del documento.** Actualizar `frontend/index.html` con `lang="es-CL"`, el título exacto, descripción y color de tema; eliminar la referencia al favicon inexistente. Validación: el HTML de build contiene esos metadatos y no enlaza un favicon ausente.
- [x] **3. Aplicar shell y controles semánticos.** Ajustar acceso, barra superior, navegación, panels de tabs y selector de ancla con landmarks, encabezados, relaciones ARIA y `RadioGroup` MUI. Añadir iconos MUI a acciones con texto visible. Validación: pruebas encuentran `main`, tabs/paneles y grupo de radio por roles y nombres.
- [x] **4. Crear el patrón de tabla responsive.** Sustituir las tablas HTML por componentes MUI y un patrón reutilizable con nombre accesible, scroll contenido, wrapping textual y columnas numéricas estables. Validación: movimientos, categorías, detalle mensual y comercios conservan todas sus columnas y no usan el mínimo global de 720 px.
- [x] **5. Corregir Comercios principales y contenido largo.** Aplicar `minWidth: 0` a grids relevantes, ancho de tabla específico y contenedor de scroll accesible; revisar recurrencias y descripciones largas. Validación: una prueba con comercio largo conserva el contenido dentro de la tarjeta y Playwright no detecta overflow del documento.
- [x] **6. Reorganizar dashboard y gráficos.** Convertir KPIs en franja agrupada, ajustar padding y jerarquía de señales/cobertura, y configurar paletas y formatters CLP de gráficos sin modificar data API. Validación: pruebas de dashboard siguen encontrando avisos, highlights, métricas, gráficos y tablas equivalentes.
- [x] **7. Ampliar cobertura de pruebas y verificar.** Actualizar pruebas unitarias y E2E para semántica, iconos, contenido largo y anchos de móvil/escritorio. Ejecutar lint, unitarias, build, E2E y `git diff --check`. Validación: todos los comandos terminan correctamente.
